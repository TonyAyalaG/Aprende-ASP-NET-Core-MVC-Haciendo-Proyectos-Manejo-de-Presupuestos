
@{
	ViewData["Title"] = "Reporte Excel";
	var hoy = DateTime.Today;
	var haceUnMes = hoy.AddMonths(-1);
	var mesActual = hoy.Month;
	var anoActual = hoy.Year;
	var mesAnterior = haceUnMes.Month;
	var anoAnterior = haceUnMes.Year;
}
<partial name="_Submenu" Model="SubMenuTransacciones.Excel" />

<h1>Exportar Transacciones a Excel</h1>

<div class="mb-3">
	<a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-ano="@anoActual" class="btn btn-success">Exportar Mes Actual</a> ~ @hoy.ToString("MMMM yyyy")
</div>

<div class="mb-3">
	<a asp-action="ExportarExcelPorMes" asp-route-mes="@mesAnterior" asp-route-ano="@anoAnterior" class="btn btn-success">Exportar Mes Anterior</a> ~ @haceUnMes.ToString("MMMM yyyy")
</div>

<div class="mb-3">
	<a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-ano="@anoActual" class="btn btn-success">Exportar Año Actual</a> ~ @hoy.ToString("MMMM yyyy")
</div>

<div class="mb-3">
	<a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-ano="@anoActual" class="btn btn-success">Exportar Año Anterior</a> ~ @hoy.ToString("MMMM yyyy")
</div>

<div class="mb-3">
	<a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-ano="@anoActual" class="btn btn-success">Exportar Todo</a>
</div>